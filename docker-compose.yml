services:
  app:
    build: .
    command: bash start.sh
    restart: on-failure
    network_mode: "host"
    depends_on:
      - bgutil-pot

  bgutil-pot:
    image: brainicism/bgutil-ytdlp-pot-provider:latest
    ports:
      - "8080:8080"
    restart: unless-stopped
